import styled from 'styled-components';
import { colors } from '../../const/theme';

export const TimeMinContainer = styled.div`
  width: 50px;
  display: flex;
  gap: 10px;
  color: ${colors['gray-600']};
  font-size: 16px;
  align-items: baseline;
  margin: 0 20px 0 auto;
`;
